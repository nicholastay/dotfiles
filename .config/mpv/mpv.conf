volume=80

# For Discord screen share (KDE) to work...
ao=pulse

vo=gpu-next
hwdec=vaapi
gpu-context=wayland
hdr-compute-peak=no
gpu-context=waylandvk
gpu-api=vulkan

script-opts=ytdl_hook-ytdl_path=yt-dlp
ytdl-format=bestvideo[height<=?720][fps<=?30][vcodec!=?vp9]+bestaudio/best
